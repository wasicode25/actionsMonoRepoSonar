[tox]
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
commands =    
    #coverage run -m pytest 
    #coverage xml
    python -c "print('Descomentar las lineas de arriba cuando se tengan implementados los tests de python')"
    
[coverage:run]
relative_files = True