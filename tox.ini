[tox]
#envlist = py311
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
commands =    
    coverage run -m pytest || true
    coverage xml || true

ignore_errors = True

[coverage:run]
relative_files = True
#source = servipagos/src/
#branch = True

[coverage:report]
ignore_errors = True